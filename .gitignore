# Binaries
compartment-user
compartment-root
tests/probes/deny_probe

# Build artifacts
*.o
*.d
*.a
*.so

# ...but the test discovery directories are not build artifacts. Without
# these, "*.d" silently swallows every suite dropped into them.
!tests/scripts/root.d/
!tests/scripts/rootless.d/

# Generated headers (config.py output)
config.h
compartment-root-config.h

# Core dumps
core
core.*
vgcore.*

# Test output
tests/output/

# Review / analysis artifacts
review-isolation-tools.md
vs-*.md

# Archive (tracked on archive/* branches, not main)
archive/

# Editor
*.swp
*~
.vscode/
