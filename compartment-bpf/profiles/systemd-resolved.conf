# Profile: systemd-resolved
#
# Daemon name      : systemd-resolved
# Version on VM    : systemd 259 (259.5-0ubuntu3)
# Systemd unit     : systemd-resolved.service
# ExecStart        : /usr/lib/systemd/systemd-resolved
# Binary path      : /usr/lib/systemd/systemd-resolved
#
# Main config paths (sealed):
#   /etc/systemd/resolved.conf
#   /usr/lib/systemd/resolved.conf.d    (vendor drop-in — contains
#                                        00-disable-mdns.conf and
#                                        cache-no-negative.conf on Resolute;
#                                        per Phase E audit)
#   (no /etc/systemd/resolved.conf.d/ on stock Resolute)
#
# Runtime state (NOT sealed):
#   /run/systemd/resolve                (resolv.conf, stub-resolv.conf, io.systemd.Resolve socket)
#   /etc/resolv.conf                    (symlink to /run/systemd/resolve/stub-resolv.conf — runtime)
#
# Liveness check:
#   systemctl is-active systemd-resolved && \
#       resolvectl status --no-pager >/dev/null && \
#       getent hosts localhost | grep -q '127\.'
#
# Note: /etc/resolv.conf is a symlink to /run/... on Resolute and is
# managed at runtime — it must NOT be sealed or DNS breaks during link
# state changes. The configuration surface is /etc/systemd/resolved.conf.

# Daemon binary
seal /usr/lib/systemd/systemd-resolved       full

# Main config + vendor drop-in dir
seal /etc/systemd/resolved.conf              full
seal /usr/lib/systemd/resolved.conf.d        full

# Liveness: systemctl is-active systemd-resolved && \
#               resolvectl status --no-pager >/dev/null && \
#               getent hosts localhost | grep -q '127\.'
