# ED-1 fixture 01: a single actor declaration and a single seal that
# binds to it. Must parse cleanly. Paths chosen to be syntactically
# absolute; --parse-only does not stat them.
actor aide = /usr/sbin/aide

seal /var/lib/aide/aide.db no-write,no-unlink,no-rename actor=aide
