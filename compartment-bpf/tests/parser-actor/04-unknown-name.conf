# ED-1 fixture 04: seal references actor 'ghost' that was never
# declared in this profile. MUST fail with "unknown actor".
actor aide = /usr/sbin/aide

seal /var/lib/aide/aide.db no-write actor=ghost
