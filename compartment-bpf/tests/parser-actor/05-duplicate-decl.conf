# ED-1 fixture 05: actor name declared twice. MUST fail with
# "duplicate actor declaration".
actor aide = /usr/sbin/aide
actor aide = /usr/bin/aide-check

seal /var/lib/aide/aide.db no-write actor=aide
