# ED-1 fixture 03: forward reference (seal uses actor=foo before the
# `actor foo = ...` declaration). MUST fail with "unknown actor".
seal /var/lib/aide/aide.db no-write actor=aide

actor aide = /usr/sbin/aide
