# test-env-deny.conf — environment deny-list
#
# Strips specific dangerous vars, keeps everything else

env-deny LD_PRELOAD
env-deny LD_LIBRARY_PATH
env-deny LD_AUDIT
env-deny SECRET_TOKEN

landlock off
seccomp off
no-new-privs on
env-sanitize on
